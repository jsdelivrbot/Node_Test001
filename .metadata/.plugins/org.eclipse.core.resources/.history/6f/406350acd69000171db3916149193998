export default function test_redcr(state, action){
	
	
	switch(action.type){
		case "ADD_TODO":
			return Object.assign({},state, {
							                  todos: [
							                	  {//add new todo info},
							                	  ...state.todos
							                  ]
							                  
											});
		default:
			return state;
	}
	
}